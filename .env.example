# Supabase (Create React App requires REACT_APP_ prefix)
#
# Setup:
# 1) Copy this file to .env.local (recommended) or .env
# 2) Fill in the values below
# 3) Restart `yarn start`

# Supabase project URL (must include https://)
REACT_APP_SUPABASE_URL=https://<project-ref>.supabase.co

# Supabase anon/public API key
REACT_APP_SUPABASE_ANON_KEY=<your-anon-key>

# Optional: if your server expects the Supabase JWT on requests,
# the front-end will send it as `Authorization: Bearer <access_token>` when logged in.
